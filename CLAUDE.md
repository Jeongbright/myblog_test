# my_blog

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 목표

- 마크다운으로 글을 작성하면 HTML 블로그 페이지로 변환되어 보여지는 개인 블로그
- 깔끔하고 읽기 좋은 타이포그래피 중심 디자인
- 다크모드 지원 (시스템 설정 감지 + 수동 토글)
- 모바일 반응형 (좁은 화면에서도 읽기 편한 레이아웃)

## 기술 스택 / 제약

- **프레임워크 없음.** 순수 HTML, CSS, JavaScript(vanilla)만 사용.
- 빌드 도구(webpack, vite 등)나 npm 패키지 의존을 최소화. 필요 시 마크다운 파싱 라이브러리 정도만 CDN으로 로드하는 것은 허용하되, 가능하면 직접 파싱하는 방향도 검토.
- 서버 사이드 언어(Node, Python 등)는 "빌드 스크립트"(마크다운 → HTML 변환)에만 사용 가능. 배포되는 결과물은 정적 파일이어야 함.

## 구조

```
my_blog/
  index.html          # 글 목록 페이지
  post.html           # 글 상세 페이지 (?slug=xxx 로 글 식별)
  css/
    style.css         # 공통 스타일, CSS 변수 기반 라이트/다크 테마
  js/
    theme.js          # 다크모드 토글/저장 (index, post 공통)
    main.js           # index.html: posts.json 로드 후 목록 렌더링
    post.js           # post.html: slug로 글 찾아 md 로드 + 렌더링
  posts/
    posts.json        # 글 메타데이터 목록 (slug, title, date, summary, file)
    2026-09-21-*.md   # 원본 마크다운 글
```

- 마크다운 → HTML 변환은 빌드 스크립트 없이 브라우저에서 `marked.js`(CDN)로 실시간 파싱
- 글 추가 흐름: `posts/`에 `.md` 파일 추가 + `posts.json`에 메타데이터 한 줄 추가
- **마크다운 본문에 `#` 최상위 제목을 넣지 않음.** 제목은 `posts.json`의 `title`을 페이지 템플릿이 렌더링하므로, 본문에 또 넣으면 제목이 중복 표시됨. 본문은 `##`(h2)부터 시작
- 로컬에서 열 때는 `fetch()`로 `posts.json`/`.md`를 읽기 때문에 `file://`로 직접 열면 동작하지 않음. 정적 서버(예: `npx http-server -p 8000 -c-1 .`)로 띄워서 확인

## 디자인 원칙

- 본문은 읽기 편한 폭(약 65-75ch)으로 제한, 넉넉한 줄간격(1.6~1.8)
- 색상은 CSS 변수(`--bg`, `--text`, `--accent` 등)로 관리하고 `prefers-color-scheme`과 `data-theme` 속성 토글로 다크모드 전환
- 폰트는 시스템 폰트 스택 우선 (불필요한 외부 폰트 로드 지양)
- 모바일 브레이크포인트는 필요한 지점에서만 최소한으로 사용 (모바일 퍼스트 지향)

## 코딩 컨벤션

- 주석은 최소화하고, 코드 자체로 의도가 드러나게 작성
- CSS는 컴포넌트별로 섹션을 나누고 CSS 변수로 테마 값을 중앙 관리
- JS는 프레임워크 없이 순수 DOM API 사용, 불필요한 추상화 지양
- 새 글 추가는 `posts/` 에 마크다운 파일만 추가하면 되는 흐름을 목표로 함
